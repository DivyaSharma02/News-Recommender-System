# News-Recommender-System
NewsRadar is a real-time news discovery tool that scrapes live headlines from news outlets. Unlike basic keyword searches, it employs TF-IDF (Term Frequency-Inverse Document Frequency) and Cosine Similarity to rank articles based on their semantic relevance to a user's query, providing a "Match Score" for every result.
